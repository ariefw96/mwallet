spring:
  application:
    name: MWALLET-TRANSACTION

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761/eureka}
    fetch-registry: true
    register-with-eureka: true
